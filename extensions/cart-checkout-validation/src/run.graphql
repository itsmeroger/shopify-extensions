query RunInput {
  cart {
    lines {
      quantity

      merchandise: sellingPlanAllocation {
        sellingPlan {
          id
          name
          recurringDeliveries
        }
      }
    }
  }
}
